{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "pre:install": "npm config set //npm.pkg.github.com/:_authToken=$GH_PERSONAL_ACCESS_TOKEN",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.4.0"
  },
  "dependencies": {
    "@huggydigital/socket": "^0.0.3"
  }
}